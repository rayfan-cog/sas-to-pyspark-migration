[pytest]
testpaths = tests
# -ra always summarizes skips/xfails, so a skipped parity module can never hide
# behind a green run.
addopts = -ra
markers =
    golden_contract: validates the golden SAS output set before parity comparisons
